# ── ClawStack ──────────────────────────────────────────────────────────────
# Domain for the admin portal (e.g. clawstack.yourdomain.com)
BASE_DOMAIN=clawstack.yourdomain.com

# Email for Let's Encrypt TLS certificates
CADDY_EMAIL=you@yourdomain.com

# Admin portal login
ADMIN_USER=admin
ADMIN_PASS=changeme

# OpenClaw image to use for new instances
OPENCLAW_IMAGE=ghcr.io/openclaw/openclaw:latest

# Timezone for containers
TZ=Europe/Stockholm

# ── Storage ─────────────────────────────────────────────────────────────────
# Where to store OpenClaw instance data (default: ./instances on system disk)
# Options:
#   ./instances              - Relative path (system disk, default)
#   /var/clawstack/instances - Absolute path on system disk
#   /mnt/storage/instances   - Separate mounted volume
# See docs/STORAGE.md for details
#INSTANCES_HOST_DIR=./instances

# ── Private LLM for Claude CLI (optional) ─────────────────────────────────
#ANTHROPIC_BASE_URL=http://your-llm-host
#ANTHROPIC_AUTH_TOKEN=your-token
#ANTHROPIC_API_KEY=
#CLAUDE_DEFAULT_MODEL=your-model
